name: umrany

x-app-image: &app-image
  build:
    context: .
    dockerfile: docker/app/Dockerfile
  image: umrany_app:local
  working_dir: /var/www/html
  volumes:
    - .:/var/www/html
  env_file:
    - .env
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_started

services:
  app:
    <<: *app-image
    container_name: umrany_app
    ports:
      - "8000:8000"

  horizon:
    <<: *app-image
    container_name: umrany_horizon
    command: php artisan horizon
    ports: []

  reverb:
    <<: *app-image
    container_name: umrany_reverb
    command: php artisan reverb:start --host=0.0.0.0 --port=8080
    ports:
      - "8080:8080"

  scheduler:
    <<: *app-image
    container_name: umrany_scheduler
    command: sh -c "while true; do php artisan schedule:run --verbose --no-interaction; sleep 60; done"
    ports: []

  nginx:
    image: nginx:1.27-alpine
    container_name: umrany_nginx
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - .:/var/www/html:ro
    ports:
      - "80:80"
    depends_on:
      - app
      - reverb

  postgres:
    image: postgres:15-alpine
    container_name: umrany_postgres
    environment:
      POSTGRES_DB: ${DB_DATABASE:-umrany}
      POSTGRES_USER: ${DB_USERNAME:-umrany}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-umrany}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-umrany} -d ${DB_DATABASE:-umrany}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7.2-alpine
    container_name: umrany_redis
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"

volumes:
  postgres_data:
    name: umrany_postgres_data
  redis_data:
    name: umrany_redis_data
