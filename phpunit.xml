<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Modules">
            <directory>Modules/*/tests/Unit</directory>
            <directory>Modules/*/tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            These MUST be <server> entries, not <env> (even with force="true") — confirmed by
            direct reproduction, not assumption. docker-compose.yml injects .env via `env_file:`,
            which sets these as real OS environment variables inside the app container, and this
            app's env resolution (Laravel's dotenv adapter chain) reads $_SERVER ahead of
            $_ENV/getenv(). PHPUnit's <env> directive — including force="true", which only
            affects $_ENV/putenv(), never $_SERVER — silently no-ops against that: without the
            <server> form below, app()->environment() reports "local" and
            DB::connection()->getDriverName() reports "pgsql" inside an actual test run, meaning
            the entire suite was running against the real dev Postgres database instead of an
            isolated in-memory SQLite one. If this ever needs revisiting, reproduce with a throwaway
            test that dumps $_SERVER[...]/config('database.default') before changing it back.
        -->
        <server name="APP_ENV" value="testing"/>
        <server name="APP_MAINTENANCE_DRIVER" value="file"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="BROADCAST_CONNECTION" value="null"/>
        <server name="CACHE_STORE" value="array"/>
        <server name="DB_CONNECTION" value="sqlite"/>
        <server name="DB_DATABASE" value=":memory:"/>
        <server name="DB_URL" value=""/>
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="PULSE_ENABLED" value="false"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
        <server name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
